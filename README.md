# portfolio
this is a portfolio website which is made with the Python technology using Django Rest Framework
python -m venv myenv
env/Scripts/activate
now install django--> pip install django
now install django rest framework--> pip install djangorestframework

